/**
 * Load unit tests that require a jboss server to be running.
 */
define([
    // note: using the .js extension forces requireJS to look in the same directory.
    "cookieTest.js",
    "analyticsTest.js",
]);
